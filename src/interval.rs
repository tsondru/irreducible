//! [DEPRECATED v0.6.3] Re-exports of [`catgraph_physics::interval`].
//!
//! The implementation moved to `catgraph-physics` in v0.3.0 (see that
//! crate's CHANGELOG cross-repo follow-up). This module will be **removed
//! entirely in irreducible v0.7.0**; migrate to `catgraph_physics::interval`
//! at your earliest convenience.

pub use catgraph_physics::interval::{DiscreteInterval, ParallelIntervals};
